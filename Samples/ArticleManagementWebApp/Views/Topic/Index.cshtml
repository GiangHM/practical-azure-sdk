@{
    ViewData["Title"] = "Home Page";
}

<style>
    .todo-item {
        background-color: white;
        border-radius: 5px;
        margin-bottom: 15px;
        padding: 15px;
        padding-left: 20px;
        padding-right: 20px;
        box-shadow: 0px 0px 10px #e2e2e2;
        text-align: left;
        cursor: pointer;
        display:flex;
        font-size:13pt;
    }

    h1 {
        margin-bottom: 50px;
    }
</style>

<div class="text-center">
    <h1 class="display-4">Topic Management!</h1>
    <h5>You are viewing your app for the @ViewBag.Counter time</h5>

    <div style="display: flex">
        <div style="flex:1"></div>
        <div style="flex:2">
            @if (ViewBag.Topics == null || ViewBag.Topics.Count == 0)
            {
                <div>No topics :(</div>
            }
            @foreach (var item in ViewBag.Topics)
            {
                <div class="todo-item">
                    <div style="flex:12">
                        @item.Name
                    </div>
                    <div>
                        <img style="width:22px;flex:1" src="~/icons/bin.png" />
                    </div>
                </div>
            }
        </div>
        <div style="flex:1"></div>
    </div>
    <div>
    </div>
</div>